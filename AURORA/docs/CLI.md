# CLI

Entrada operacional:

```powershell
cd D:\ISIS_IA\AURORA
python -m aurora.cli status
```

Comandos:

```powershell
python -m aurora.cli status
python -m aurora.cli route "corrija este codigo"
python -m aurora.cli memory-add "AURORA usa memoria local" --confirm
python -m aurora.cli memory-search "AURORA"
python -m aurora.cli skills --json
python -m aurora.cli skill-run system_info
python -m aurora.cli voice-mock --transcript "qual o status"
```

Saida: JSON para automacao.

Sem servico online por padrao.
